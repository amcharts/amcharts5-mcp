---
title: "IClasses"
type: "interface"
source: "https://www.amcharts.com/docs/v5/reference/iclasses/"
scraped: "2026-03-15"
---

Inheritance

## Properties

- **AccelerationBands** (`() => Promise`) — (no description)
- **AccumulationDistribution** (`() => Promise`) — (no description)
- **AccumulativeSwingIndex** (`() => Promise`) — (no description)
- **Annotator** (`() => Promise`) — (no description)
- **ArcDiagram** (`() => Promise`) — (no description)
- **ArcDiagramLink** (`() => Promise`) — (no description)
- **ArcDiagramNodes** (`() => Promise`) — (no description)
- **Aroon** (`() => Promise`) — (no description)
- **AverageSeries** (`() => Promise`) — (no description)
- **AverageTrueRange** (`() => Promise`) — (no description)
- **AwesomeOscillator** (`() => Promise`) — (no description)
- **Axis** (`() => Promise`) — (no description)
- **AxisBullet** (`() => Promise`) — (no description)
- **AxisLabel** (`() => Promise`) — (no description)
- **AxisLabelRadial** (`() => Promise`) — (no description)
- **AxisRenderer** (`() => Promise`) — (no description)
- **AxisRendererCircular** (`() => Promise`) — (no description)
- **AxisRendererCurveX** (`() => Promise`) — (no description)
- **AxisRendererCurveY** (`() => Promise`) — (no description)
- **AxisRendererRadial** (`() => Promise`) — (no description)
- **AxisRendererX** (`() => Promise`) — (no description)
- **AxisRendererY** (`() => Promise`) — (no description)
- **AxisTick** (`() => Promise`) — (no description)
- **BaseColumnSeries** (`() => Promise`) — (no description)
- **BollingerBands** (`() => Promise`) — (no description)
- **BreadcrumbBar** (`() => Promise`) — (no description)
- **BullBearPower** (`() => Promise`) — (no description)
- **Bullet** (`() => Promise`) — (no description)
- **Button** (`() => Promise`) — (no description)
- **CalloutSeries** (`() => Promise`) — (no description)
- **Candlestick** (`() => Promise`) — (no description)
- **CandlestickSeries** (`() => Promise`) — (no description)
- **CategoryAxis** (`() => Promise`) — (no description)
- **CategoryDateAxis** (`() => Promise`) — (no description)
- **ChaikinMoneyFlow** (`() => Promise`) — (no description)
- **ChaikinOscillator** (`() => Promise`) — (no description)
- **Chart** (`() => Promise`) — (no description)
- **ChartIndicator** (`() => Promise`) — (no description)
- **Chord** (`() => Promise`) — (no description)
- **ChordDirected** (`() => Promise`) — (no description)
- **ChordLink** (`() => Promise`) — (no description)
- **ChordLinkDirected** (`() => Promise`) — (no description)
- **ChordNodes** (`() => Promise`) — (no description)
- **ChordNonRibbon** (`() => Promise`) — (no description)
- **Circle** (`() => Promise`) — (no description)
- **CirclePattern** (`() => Promise`) — (no description)
- **ClockHand** (`() => Promise`) — (no description)
- **ClusteredPointSeries** (`() => Promise`) — (no description)
- **ColorControl** (`() => Promise`) — (no description)
- **ColorPicker** (`() => Promise`) — (no description)
- **ColorPickerButton** (`() => Promise`) — (no description)
- **ColorSet** (`() => Promise`) — (no description)
- **ColumnSeries** (`() => Promise`) — (no description)
- **CommodityChannelIndex** (`() => Promise`) — (no description)
- **ComparisonControl** (`() => Promise`) — (no description)
- **Component** (`() => Promise`) — (no description)
- **ConfirmButton** (`() => Promise`) — (no description)
- **Container** (`() => Promise`) — (no description)
- **CurveChart** (`() => Promise`) — (no description)
- **CurveColumnSeries** (`() => Promise`) — (no description)
- **CurveCursor** (`() => Promise`) — (no description)
- **CurveLineSeries** (`() => Promise`) — (no description)
- **DataSaveControl** (`() => Promise`) — (no description)
- **DateAxis** (`() => Promise`) — (no description)
- **DateRangeSelector** (`() => Promise`) — (no description)
- **DisparityIndex** (`() => Promise`) — (no description)
- **DoodleSeries** (`() => Promise`) — (no description)
- **DrawingControl** (`() => Promise`) — (no description)
- **DrawingSeries** (`() => Promise`) — (no description)
- **DrawingToolControl** (`() => Promise`) — (no description)
- **Dropdown** (`() => Promise`) — (no description)
- **DropdownColors** (`() => Promise`) — (no description)
- **DropdownControl** (`() => Promise`) — (no description)
- **DropdownList** (`() => Promise`) — (no description)
- **DropdownListControl** (`() => Promise`) — (no description)
- **DurationAxis** (`() => Promise`) — (no description)
- **EditableAxisLabel** (`() => Promise`) — (no description)
- **EditableLabel** (`() => Promise`) — (no description)
- **Ellipse** (`() => Promise`) — (no description)
- **EllipseSeries** (`() => Promise`) — (no description)
- **Entity** (`() => Promise`) — (no description)
- **Exporting** (`() => Promise`) — (no description)
- **ExportingMenu** (`() => Promise`) — (no description)
- **FibonacciSeries** (`() => Promise`) — (no description)
- **FibonacciTimezoneSeries** (`() => Promise`) — (no description)
- **Flow** (`() => Promise`) — (no description)
- **FlowLink** (`() => Promise`) — (no description)
- **FlowNode** (`() => Promise`) — (no description)
- **FlowNodes** (`() => Promise`) — (no description)
- **ForceDirected** (`() => Promise`) — (no description)
- **FunnelSeries** (`() => Promise`) — (no description)
- **FunnelSlice** (`() => Promise`) — (no description)
- **Gantt** (`() => Promise`) — (no description)
- **GanttCategoryAxis** (`() => Promise`) — (no description)
- **GanttCategoryAxisRenderer** (`() => Promise`) — (no description)
- **GanttDateAxis** (`() => Promise`) — (no description)
- **GanttDateAxisRenderer** (`() => Promise`) — (no description)
- **GanttSeries** (`() => Promise`) — (no description)
- **GaplessDateAxis** (`() => Promise`) — (no description)
- **Gradient** (`() => Promise`) — (no description)
- **GrainPattern** (`() => Promise`) — (no description)
- **Graphics** (`() => Promise`) — (no description)
- **GraticuleSeries** (`() => Promise`) — (no description)
- **Grid** (`() => Promise`) — (no description)
- **GridLayout** (`() => Promise`) — (no description)
- **HeatLegend** (`() => Promise`) — (no description)
- **HeikinAshi** (`() => Promise`) — (no description)
- **Hierarchy** (`() => Promise`) — (no description)
- **HierarchyLink** (`() => Promise`) — (no description)
- **HierarchyNode** (`() => Promise`) — (no description)
- **HorizontalLayout** (`() => Promise`) — (no description)
- **HorizontalLineSeries** (`() => Promise`) — (no description)
- **HorizontalRaySeries** (`() => Promise`) — (no description)
- **IconControl** (`() => Promise`) — (no description)
- **IconSeries** (`() => Promise`) — (no description)
- **Indicator** (`() => Promise`) — (no description)
- **IndicatorControl** (`() => Promise`) — (no description)
- **InterfaceColors** (`() => Promise`) — (no description)
- **IntervalControl** (`() => Promise`) — (no description)
- **Label** (`() => Promise`) — (no description)
- **LabelSeries** (`() => Promise`) — (no description)
- **Layout** (`() => Promise`) — (no description)
- **Legend** (`() => Promise`) — (no description)
- **Line** (`() => Promise`) — (no description)
- **LineArrowSeries** (`() => Promise`) — (no description)
- **LinePattern** (`() => Promise`) — (no description)
- **LineSeries** (`() => Promise`) — (no description)
- **LinearGradient** (`() => Promise`) — (no description)
- **Link** (`() => Promise`) — (no description)
- **LinkedHierarchy** (`() => Promise`) — (no description)
- **LinkedHierarchyNode** (`() => Promise`) — (no description)
- **MACD** (`() => Promise`) — (no description)
- **MACross** (`() => Promise`) — (no description)
- **MapChart** (`() => Promise`) — (no description)
- **MapLine** (`() => Promise`) — (no description)
- **MapLineSeries** (`() => Promise`) — (no description)
- **MapPointSeries** (`() => Promise`) — (no description)
- **MapPolygon** (`() => Promise`) — (no description)
- **MapPolygonSeries** (`() => Promise`) — (no description)
- **MapSeries** (`() => Promise`) — (no description)
- **Measure** (`() => Promise`) — (no description)
- **MedianPrice** (`() => Promise`) — (no description)
- **Modal** (`() => Promise`) — (no description)
- **Momentum** (`() => Promise`) — (no description)
- **MovingAverage** (`() => Promise`) — (no description)
- **MovingAverageDeviation** (`() => Promise`) — (no description)
- **MovingAverageEnvelope** (`() => Promise`) — (no description)
- **NumericStepper** (`() => Promise`) — (no description)
- **OHLC** (`() => Promise`) — (no description)
- **OHLCSeries** (`() => Promise`) — (no description)
- **OnBalanceVolume** (`() => Promise`) — (no description)
- **OrthogonalLine** (`() => Promise`) — (no description)
- **OverboughtOversold** (`() => Promise`) — (no description)
- **PVT** (`() => Promise`) — (no description)
- **Pack** (`() => Promise`) — (no description)
- **PanelControls** (`() => Promise`) — (no description)
- **ParallelChannelSeries** (`() => Promise`) — (no description)
- **Partition** (`() => Promise`) — (no description)
- **PathPattern** (`() => Promise`) — (no description)
- **Pattern** (`() => Promise`) — (no description)
- **PatternSet** (`() => Promise`) — (no description)
- **PercentChart** (`() => Promise`) — (no description)
- **PercentSeries** (`() => Promise`) — (no description)
- **PeriodSelector** (`() => Promise`) — (no description)
- **PictorialStackedSeries** (`() => Promise`) — (no description)
- **Picture** (`() => Promise`) — (no description)
- **PicturePattern** (`() => Promise`) — (no description)
- **PieChart** (`() => Promise`) — (no description)
- **PieSeries** (`() => Promise`) — (no description)
- **PointedRectangle** (`() => Promise`) — (no description)
- **Polygon** (`() => Promise`) — (no description)
- **PolylineSeries** (`() => Promise`) — (no description)
- **ProgressPie** (`() => Promise`) — (no description)
- **PyramidSeries** (`() => Promise`) — (no description)
- **QuadrantLineSeries** (`() => Promise`) — (no description)
- **RadarChart** (`() => Promise`) — (no description)
- **RadarColumnSeries** (`() => Promise`) — (no description)
- **RadarCursor** (`() => Promise`) — (no description)
- **RadarLineSeries** (`() => Promise`) — (no description)
- **RadialGradient** (`() => Promise`) — (no description)
- **RadialLabel** (`() => Promise`) — (no description)
- **RadialText** (`() => Promise`) — (no description)
- **Rectangle** (`() => Promise`) — (no description)
- **RectanglePattern** (`() => Promise`) — (no description)
- **RectangleSeries** (`() => Promise`) — (no description)
- **RegressionSeries** (`() => Promise`) — (no description)
- **RelativeStrengthIndex** (`() => Promise`) — (no description)
- **ResetControl** (`() => Promise`) — (no description)
- **RoundedRectangle** (`() => Promise`) — (no description)
- **Sankey** (`() => Promise`) — (no description)
- **SankeyLink** (`() => Promise`) — (no description)
- **SankeyNodes** (`() => Promise`) — (no description)
- **Scrollbar** (`() => Promise`) — (no description)
- **SerialChart** (`() => Promise`) — (no description)
- **Serializer** (`() => Promise`) — (no description)
- **Series** (`() => Promise`) — (no description)
- **SeriesTypeControl** (`() => Promise`) — (no description)
- **SerpentineChart** (`() => Promise`) — (no description)
- **SettingsControl** (`() => Promise`) — (no description)
- **SettingsModal** (`() => Promise`) — (no description)
- **SimpleLineSeries** (`() => Promise`) — (no description)
- **Slice** (`() => Promise`) — (no description)
- **SliceGrouper** (`() => Promise`) — (no description)
- **SlicedChart** (`() => Promise`) — (no description)
- **Slider** (`() => Promise`) — (no description)
- **SmoothedRadarLineSeries** (`() => Promise`) — (no description)
- **SmoothedXLineSeries** (`() => Promise`) — (no description)
- **SmoothedXYLineSeries** (`() => Promise`) — (no description)
- **SmoothedYLineSeries** (`() => Promise`) — (no description)
- **SpiralChart** (`() => Promise`) — (no description)
- **Sprite** (`() => Promise`) — (no description)
- **SpriteResizer** (`() => Promise`) — (no description)
- **StandardDeviation** (`() => Promise`) — (no description)
- **Star** (`() => Promise`) — (no description)
- **StepLineSeries** (`() => Promise`) — (no description)
- **StochasticMomentumIndex** (`() => Promise`) — (no description)
- **StochasticOscillator** (`() => Promise`) — (no description)
- **StockChart** (`() => Promise`) — (no description)
- **StockControl** (`() => Promise`) — (no description)
- **StockLegend** (`() => Promise`) — (no description)
- **StockPanel** (`() => Promise`) — (no description)
- **StockToolbar** (`() => Promise`) — (no description)
- **Sunburst** (`() => Promise`) — (no description)
- **SuperTrend** (`() => Promise`) — (no description)
- **Text** (`() => Promise`) — (no description)
- **Tick** (`() => Promise`) — (no description)
- **Tooltip** (`() => Promise`) — (no description)
- **Tree** (`() => Promise`) — (no description)
- **Treemap** (`() => Promise`) — (no description)
- **TrendLineSeries** (`() => Promise`) — (no description)
- **Triangle** (`() => Promise`) — (no description)
- **Trix** (`() => Promise`) — (no description)
- **TypicalPrice** (`() => Promise`) — (no description)
- **VWAP** (`() => Promise`) — (no description)
- **ValueAxis** (`() => Promise`) — (no description)
- **Venn** (`() => Promise`) — (no description)
- **VerticalLayout** (`() => Promise`) — (no description)
- **VerticalLineSeries** (`() => Promise`) — (no description)
- **Volume** (`() => Promise`) — (no description)
- **VolumeProfile** (`() => Promise`) — (no description)
- **VoronoiTreemap** (`() => Promise`) — (no description)
- **WilliamsR** (`() => Promise`) — (no description)
- **WordCloud** (`() => Promise`) — (no description)
- **XYChart** (`() => Promise`) — (no description)
- **XYChartScrollbar** (`() => Promise`) — (no description)
- **XYCursor** (`() => Promise`) — (no description)
- **XYSeries** (`() => Promise`) — (no description)
- **ZigZag** (`() => Promise`) — (no description)
- **ZoomControl** (`() => Promise`) — (no description)
- **ZoomTools** (`() => Promise`) — (no description)

