---
title: "IRenderer"
type: "interface"
source: "https://www.amcharts.com/docs/v5/reference/irenderer/"
scraped: "2026-03-15"
---

Inheritance

## Inheritance

Extends: any

> **Note:** This class also inherits all settings, properties, methods, and events from any (and its ancestors). Use `get_doc` or `get_core_reference` with the parent class name to see inherited members.

## Properties

- **debugGhostView** (`boolean`) — (no description)
- **interactionsEnabled** (`boolean`) — (no description)
- **resolution** (`number`) — (no description)
- **tapToActivate** (`boolean`) — (no description)
- **tapToActivateTimeout** (`number`) — (no description)

