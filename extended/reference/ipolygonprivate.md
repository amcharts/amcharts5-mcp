---
title: "IPolygonPrivate"
type: "interface"
source: "https://www.amcharts.com/docs/v5/reference/ipolygonprivate/"
scraped: "2026-03-15"
---

Inheritance

## Inheritance

Extends: IGraphicsPrivate

> **Note:** This class also inherits all settings, properties, methods, and events from IGraphicsPrivate (and its ancestors). Use `get_doc` or `get_core_reference` with the parent class name to see inherited members.

## Properties

- **maxX** (`undefined | number`) — (no description)
- **maxY** (`undefined | number`) — (no description)
- **minX** (`undefined | number`) — (no description)
- **minY** (`undefined | number`) — (no description)
- **morphProgress** (`undefined | number`) — (no description)
- **points** (`Array`) — (no description)
- **previousPoints** (`Array`) — (no description)
