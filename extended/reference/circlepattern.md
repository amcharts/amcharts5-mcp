---
title: "CirclePattern"
type: "class"
source: "https://www.amcharts.com/docs/v5/reference/circlepattern/"
scraped: "2026-03-15"
---

Circle pattern.

## Import

```javascript
// Import CirclePattern
import * as am5 from "@amcharts/amcharts5"
```

## Inheritance

Extends: Pattern

> **Note:** This class also inherits all settings, properties, methods, and events from Pattern (and its ancestors). Use `get_doc` or `get_core_reference` with the parent class name to see inherited members.

## Settings

- **centered** (`undefined | false | true`) — Default true Center circles.
- **checkered** (`undefined | false | true`) — Default false If set to true, will place every second circle, creating checkered pattern.
- **gap** (`undefined | number`) — Default 3 Gap between circles, in pixels.
- **radius** (`undefined | number`) — Default 3 Radius of the circles, in pixels.
