---
title: "IDisplayObject"
type: "interface"
source: "https://www.amcharts.com/docs/v5/reference/idisplayobject/"
scraped: "2026-03-15"
---

Inheritance

## Inheritance

Extends: any
Extended by: IContainer, IGraphics, IPicture

> **Note:** This class also inherits all settings, properties, methods, and events from any (and its ancestors). Use `get_doc` or `get_core_reference` with the parent class name to see inherited members.

## Properties

- **alpha** (`number`) — (no description)
- **angle** (`number`) — (no description)
- **buttonMode** (`boolean`) — (no description)
- **cancelTouch** (`boolean`) — (no description)
- **crisp** (`boolean`) — (no description)
- **cursorOverStyle** (`undefined | string`) — (no description)
- **exportable** (`undefined | false | true`) — (no description)
- **filter** (`undefined | string`) — (no description)
- **inactive** (`boolean | null`) — (no description)
- **interactive** (`boolean`) — (no description)
- **isMeasured** (`boolean`) — (no description)
- **mask** (`IGraphics | null`) — (no description)
- **pivot** (`IPoint`) — (no description)
- **scale** (`number`) — (no description)
- **visible** (`boolean`) — (no description)
- **wheelable** (`boolean`) — (no description)
- **x** (`number`) — (no description)

